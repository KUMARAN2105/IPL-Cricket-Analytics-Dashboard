# Data Directory

Place IPL datasets in this directory.

## Recommended structure

- `data/raw/` - source files imported from external IPL datasets
- `data/processed/` - cleaned files generated by `scripts/prepare_data.py`

## Notes

- Keep raw data unchanged.
- Use scripts in `scripts/` to create processed data for Power BI.
